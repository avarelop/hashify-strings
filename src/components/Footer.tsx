
import React from 'react';

const Footer: React.FC = () => {
  return (
    <footer className="mt-16 text-center text-xs text-muted-foreground pb-8 animate-fade-in">
      <p>Designed with precision and simplicity.</p>
      <p className="mt-1">All operations are performed locally in your browser. Your data never leaves your device.</p>
    </footer>
  );
};

export default Footer;
